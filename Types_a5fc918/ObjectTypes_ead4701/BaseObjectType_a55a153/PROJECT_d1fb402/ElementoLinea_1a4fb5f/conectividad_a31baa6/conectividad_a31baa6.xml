<?xml version="1.0" encoding="UTF-8"?>
<script>
  <metadata>
    <priority>0</priority>
    <owner>root</owner>
    <runcontext>caller</runcontext>
  </metadata>
  <parameter name="connectivity" type="interval" trigger="true" relative="false" value="" offset="00:00:00" interval="00:01:00"/>
  <code><![CDATA[var estado = Ua.findNode(base + ".connectivity");
var map = Ua.findNode(base + ".connect");
var ea = Ua.findNode(base + ".estadoAnterior")
//map.result.value = estado.result.status;


	if (estado.result.status==0){
		map.result.value =	true;
	}
	else {
		map.result.value = false;
	}
	ea.result.value = map.result.value;
]]></code>
</script>
